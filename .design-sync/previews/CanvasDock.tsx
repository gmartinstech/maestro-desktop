import { CanvasDock } from '@martinstech/maestro-ds';

export const Toolbar = () => (
  <div style={{ position: 'relative', height: 120, background: 'var(--mds-bg-page)', borderRadius: 14 }}>
    <CanvasDock />
  </div>
);
