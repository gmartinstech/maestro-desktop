import { CanvasEmptyState } from '@martinstech/maestro-ds';

export const Default = () => (
  <div style={{ position: 'relative', height: 420, background: 'var(--mds-bg-page)', borderRadius: 14 }}>
    <CanvasEmptyState />
  </div>
);
