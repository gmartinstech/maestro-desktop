{
  "name": "@maestro/engine",
  "private": true,
  "version": "0.0.0",
  "description": "Maestro Studio TypeScript engine -- the strangler-fig HTTP/WS front door (ENG phase of the TXM migration). Serves /api/<name> and /ws/<name> either natively or by proxying to a spawned Python backend, per engine/src/split.ts.",
  "main": "dist/main.js",
  "scripts": {
    "prebuild": "tsc -p ../contract/tsconfig.json",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "tsx watch src/main.ts",
    "test": "vitest run",
    "pretypecheck": "tsc -p ../contract/tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.2.122",
    "@lydell/node-pty": "^1.1.0",
    "@napi-rs/keyring": "^2.0.0",
    "fastify": "^5.12.1",
    "jszip": "^3.10.1",
    "luxon": "^3.7.2",
    "ws": "^8.21.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@types/luxon": "^3.7.5",
    "@types/node": "^22.20.1",
    "@types/ws": "^8.18.1",
    "eslint": "^9.39.5",
    "globals": "^15.15.0",
    "tsx": "^4.23.13",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.66.0",
    "vitest": "^2.1.9"
  }
}
