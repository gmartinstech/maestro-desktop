---
category: Feedback
---

# Toast

The floating Dynamic Island pill — one line, at most one action. Pass it to AppShell via the toasts slot.

```tsx
<Toast tone="success" message="Workflow finished in 48s" actionLabel="View" />
```
