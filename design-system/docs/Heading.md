---
category: Typography & Layout
---

# Heading

Four sizes: 1 page title, 2 section, 3 card title, 4 label. Use `as` to keep the document outline right when the size does not match the level.

```tsx
<Heading level={1}>Dashboard</Heading>
<Heading level={3} as="h2">Recent runs</Heading>
```
