---
category: Data
---

# Tabs

Underlined tab bar. Counts go in the pill, not the label.

```tsx
<Tabs value={tab} onChange={setTab} items={[
  { id: 'general', label: 'General' },
  { id: 'runs', label: 'Runs', count: 12 },
]} />
```
