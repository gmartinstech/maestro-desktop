---
category: Forms
---

# Switch

Bare instant-apply toggle. In a settings list use SwitchRow so the label and description come with it.

```tsx
<Switch checked={on} onChange={setOn} label="Reduce motion" />
```
