---
category: Forms
---

# SwitchRow

The Settings row pattern: label, description and toggle laid out full width. Separate rows with a Divider.

```tsx
<SwitchRow label="Install updates automatically" description="Apply on next launch." checked={on} onChange={setOn} />
```
