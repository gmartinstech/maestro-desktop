---
category: Shell
---

# PageHeader

The title block every content route opens with. At most one primary Button in `actions`.

```tsx
<PageHeader title="Workflows" subtitle="Reusable multi-step runs."
  breadcrumbs={['Maestro Studio', 'Workflows']}
  actions={<Button icon={<Icon name="plus" />}>New workflow</Button>} />
```
