---
category: Typography & Layout
---

# Text

Body copy at the three tones. `muted` is metadata, `primary` is the readable body, `secondary` is the default.

```tsx
<Text>Everything your agents did today.</Text>
<Text tone="muted" size="sm">Updated 2 minutes ago</Text>
<Text mono>run_a41f92</Text>
```
