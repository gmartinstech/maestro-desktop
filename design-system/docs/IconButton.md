---
category: Primitives
---

# IconButton

Label-less square control for title bars, card headers and row actions. `label` is mandatory — it is the accessible name.

```tsx
<IconButton icon={<Icon name="refresh" />} label="Refresh" />
<IconButton icon={<Icon name="bell" />} label="Notifications" dot />
```
