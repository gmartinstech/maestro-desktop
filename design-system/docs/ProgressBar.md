---
category: Data
---

# ProgressBar

Determinate progress — update downloads, workflow completion, quota. `gold` is the update tone.

```tsx
<ProgressBar value={62} label="Downloading update" showValue tone="gold" />
```
