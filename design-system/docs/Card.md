---
category: Surfaces
---

# Card

The standard panel. `flush` removes padding so a Table can sit edge to edge inside it.

```tsx
<Card title="Recent runs" subtitle="Last 24 hours" padding="flush">
  <Table columns={cols} rows={rows} />
</Card>
```
