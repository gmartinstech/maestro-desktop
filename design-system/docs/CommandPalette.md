---
category: Shell
---

# CommandPalette

Global search and command launcher, scrimmed over the shell. Group items with the `group` field.

```tsx
<CommandPalette open query="rel" activeId="run"
  items={[{ id: 'run', label: 'Run Release Notes', group: 'Agents', shortcut: 'Enter' }]} />
```
