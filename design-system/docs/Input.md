---
category: Forms
---

# Input

Single-line field. Pass `mono` for anything the user might copy — paths, keys, model ids.

```tsx
<Input label="Endpoint" defaultValue="https://llm.martinstech.net/v1" mono hint="All traffic goes through provedor-ia." />
<Input label="Name" error="Already taken" />
```
