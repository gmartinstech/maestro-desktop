---
category: Foundations
---

# Icon

The whole icon set behind one `name` prop. 16px in buttons and nav rows, 18-20px in headers, 24px+ in empty states.

```tsx
<Icon name="agent" size={16} />
<Icon name="warning" size={20} color="var(--mds-warning)" />
```
