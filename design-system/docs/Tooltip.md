---
category: Primitives
---

# Tooltip

Hover/focus tip. Set `open` to pin it for docs and previews; leave it unset in the app.

```tsx
<Tooltip content="Stop the current run">
  <IconButton icon={<Icon name="stop" />} label="Stop" />
</Tooltip>
```
