---
category: Forms
---

# Checkbox

Use when the change needs an explicit Save. For instant-apply settings use SwitchRow.

```tsx
<Checkbox checked={on} onChange={setOn} label="Run on startup" description="Launches the agent when Maestro opens." />
```
